package com.medical_platform.config;

//import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
//import org.springframework.web.servlet.config.annotation.CorsRegistry;
//import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class CorsConfig {
	/*
	 *@Bean
	public WebMvcConfigurer corsCnfigigurer() {
	return new WebMvcConfigurer() {
		@Override
		public void addCorsMappings(CorsRegistry registry) {
			registry.addMapping("/api/**")//allows all end-points begin with /api
				.allowedOrigins("http://localhost:4200")// URL of my angular app
				.allowedMethods("GET", "POST", "PUT", "DELETE")// authorised methods
				.allowedHeaders("*");
		}
	};
} 
	 */

}
